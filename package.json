{
  "name": "aws-cron-lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:qa": "rm -rf .serverless && npx serverless deploy --aws-profile pruebas --stage qa --region us-west-2 --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.992.0"
  },
  "devDependencies": {
    "serverless": "^2.59.0"
  }
}
